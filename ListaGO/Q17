package main

import "fmt"

func main() {
	var x, y, i float64

	fmt.Print("DIGITE DOIS VALORES: ")
	fmt.Scan(&x, &y)

	if int(x)%2 == 0 {
		for i = 0; i <= y-1; i++ {
			fmt.Print(x+i*2, " ")
		}
	} else {
		fmt.Println("O PRIMEIRO NUMERO NAO E PAR")
	}
}
