package main

import (
	"fmt"
)

func main() {
	var a, b, c int
	fmt.Println("Digite 3 valores:")
	fmt.Scan(&a, &b, &c)

	if a < 10 && b < 10 && c < 10 {

		numeroFinal := a*100 + b*10 + c
		fmt.Println(numeroFinal, ",", numeroFinal*numeroFinal)
	} else {

		fmt.Println("NÚMERO INVÁLIDO")
	}
}
